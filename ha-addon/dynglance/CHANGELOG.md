# Changelog

## 1.0.0

- Initial Home Assistant add-on release, built from source against the
  `main` branch of https://github.com/trooperthorn/ha_app_dynglance.
- Ingress support, persistent `/config` (dynglance.yml) and `/data`
  (cache/database) mappings, Docker socket access for the Docker widgets.
